# Standalone deployment: builds the image and serves the game on :8017.
services:
  waldo-royale:
    build: .
    container_name: waldo-royale
    restart: unless-stopped
    ports:
      - "8017:8080"
