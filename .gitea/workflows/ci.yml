name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        run: |
          apt-get update -qq && apt-get install -y -qq build-essential curl > /dev/null
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs \
            | sh -s -- -y --profile minimal --default-toolchain stable
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"

      - name: Run test suite
        run: cargo test --workspace --exclude waldo-client

      - name: Check client compiles for wasm
        run: |
          rustup target add wasm32-unknown-unknown
          cargo check -p waldo-client --target wasm32-unknown-unknown
